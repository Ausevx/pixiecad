/* Layer 4. The full upload flow, view tags, settings, and the inline GPU
   capacity strip land here. */
export function NewJobRoute() {
  return (
    <div className="mx-auto max-w-[1400px] px-5 py-8">
      <h1 className="font-mono text-[12px] uppercase tracking-widest text-ink-dim">
        New job
      </h1>
      <p className="mt-4 font-mono text-[12px] text-ink-faint">
        Layer 4 — upload, view tags, settings, GPU capacity strip.
      </p>
    </div>
  );
}
